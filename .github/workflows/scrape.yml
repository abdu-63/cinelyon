name: Scrape Movies

on:
  # Exécution quotidienne à 00h00, 10h00 et 20h30 heure française
  schedule:
    - cron: '0 23 * * *'

  # Permet de lancer manuellement le workflow
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install -r requirements.txt
      
      - name: Run scraping script
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          THEATERS: ${{ secrets.THEATERS }}
        run: python scrape.py
      
      - name: Commit and push movies.json
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add movies.json tmdb_cache.json || true
          git diff --quiet --cached && echo "No changes to commit" || (git commit -m "Update movies.json - $(date +'%Y-%m-%d %H:%M')" && git push)
